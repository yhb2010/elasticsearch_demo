选择你感兴趣的索引 （ 或者多个索引 ）然后执行一个索引级别的 统计 API：
	GET my_index/_stats 统计 my_index 索引。
	GET my_index,another_index/_stats 使用逗号分隔索引名可以请求多个索引统计值。
	GET _all/_stats 使用特定的 _all 可以请求全部索引的统计值
实践中，节点为中心的统计还是显得更有用些。瓶颈往往是针对整个节点而言，而不是对于单个索引。因为索引一般是分布在多个节点上的，这导致以索引为中心的统计
值通常不是很有用，因为它们是从不同环境的物理机器上汇聚的数据。